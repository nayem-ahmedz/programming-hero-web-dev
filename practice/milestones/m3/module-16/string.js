const name = 'Nayem Ahmed';
const rule = "student";
const sentence = `Hey, ${name} is a ${rule}`;
const message = new String('value'); // [String : 'value'] object

console.log(message, typeof message);